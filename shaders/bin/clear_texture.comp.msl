#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

kernel void main0(texture2d<float, access::write> outTexture [[texture(0)]], uint3 gl_GlobalInvocationID [[thread_position_in_grid]])
{
    do
    {
        uint2 _109 = uint2(outTexture.get_width(), outTexture.get_height());
        bool _137;
        if (!(gl_GlobalInvocationID.x >= _109.x))
        {
            _137 = gl_GlobalInvocationID.y >= _109.y;
        }
        else
        {
            _137 = true;
        }
        if (_137)
        {
            break;
        }
        outTexture.write(float4(0.0, 0.0, 0.0, 1.0), uint2(gl_GlobalInvocationID.xy));
        break;
    } while(false);
}

